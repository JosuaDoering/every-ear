# Publish a release on a version tag (e.g. v1.2.3). Builds macOS (arm64),
# Windows (x64) and Linux (x64 + arm64 AppImage, x64 deb), uploads all to a
# single draft GitHub Release, then flips that draft to published so all
# platform assets appear together. Windows and Linux AppImage auto-update from
# the feed; macOS and Linux deb detect the new release via the GitHub API.
name: Release desktop

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    # Serialize so the two jobs don't race to create the draft release.
    strategy:
      max-parallel: 1
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: mac
          - os: windows-latest
            platform: win
          - os: ubuntu-latest
            platform: linux

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            backend/package-lock.json
            frontend/package-lock.json
            desktop/package-lock.json

      - uses: actions/setup-go@v5
        if: matrix.platform == 'mac'
        with:
          go-version: stable

      - name: Install Linux system dependencies
        if: matrix.platform == 'linux'
        run: sudo apt-get update && sudo apt-get install -y libfuse2 libgtk-3-dev libxss1 libnss3 libgbm1

      - run: npm ci
      - run: npm ci
        working-directory: backend
      - run: npm ci
        working-directory: frontend
      - run: npm ci
        working-directory: desktop

      # The published version must match the pushed tag (electron-builder names
      # the artifacts and the release tag from desktop/package.json's version).
      - name: Set version from tag
        shell: bash
        run: npm version "${GITHUB_REF_NAME#v}" --no-git-tag-version --allow-same-version
        working-directory: desktop

      - name: Fetch native binaries (LiveKit + Caddy)
        run: npm run desktop:fetch-binaries

      - name: Build backend + frontend + main
        run: npm run desktop:build

      - name: Build & publish (${{ matrix.platform }})
        run: npm run release:${{ matrix.platform }} --prefix desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-release:
    name: Publish draft release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Flip draft to published
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release edit "$GITHUB_REF_NAME" --draft=false --latest
